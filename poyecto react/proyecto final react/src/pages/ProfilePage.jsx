import { BasicPage } from "../components/BasicPage";

export const ProfilePage = () => {//exportamos el componente ProfilePage 
    return (//retornamos el componente BasicPage con title y description
        <BasicPage title="Profile Page" description="Protected rout" />
    );
}