import { BasicPage } from "../components/BasicPage";

export const SettingsPage = () => {//exportamos el componente SettingsPage
    return (//retornamos el componente BasicPage con title y description
        <BasicPage title="Paguina de configuracion" description="cambia tu configuracion" />
    );
}